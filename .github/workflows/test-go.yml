name: Test Go

on:
  workflow_call:
    inputs:
      command:
        description: 'The command to use to run the tests (defaults to go test -v -vet=all ./...)'
        required: false
        type: string
        default: go test -v -vet=all ./...
      version:
        description: 'The Go version to use'
        required: false
        type: string
      cache:
        description: 'Whether to use the cache'
        required: false
        type: boolean
        default: true

permissions: {}

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@6ec6af7d622602bd852df48848f3cae95c760a48
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            golang.org:443
            objects.githubusercontent.com:443
            proxy.golang.org:443
            storage.googleapis.com:443
            sum.golang.org:443

      - name: Init go
        uses: bytemare/actions/init-go@3bb9abafed30ae2dad9ad9b0232a4a4fe5742067
        with:
          go-version: ${{ inputs.version }}
          cache: ${{ inputs.cache }}

      - name: Run Tests
        run: ${{ inputs.command }}
